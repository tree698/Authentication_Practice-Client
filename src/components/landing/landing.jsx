import React from 'react';

const Landing = (props) => (
  <div
    style={{
      display: 'flex',
      justifyContent: 'center',
      alignItems: 'center',
      width: '100%',
      height: '100vh',
    }}
  >
    <h1>Landing Page</h1>
  </div>
);

export default Landing;
